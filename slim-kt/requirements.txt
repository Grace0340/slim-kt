# SLIM-KT dependencies.
# On AutoDL, install torch matching the image CUDA FIRST (see scripts/autodl_setup.sh);
# do not pin torch here so the CUDA build is not overwritten.

# --- core ---
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pyyaml>=6.0
tqdm>=4.66

# --- teacher: semantic embeddings + LLM attribute extraction ---
sentence-transformers>=2.6      # semantic item embeddings (frozen encoder)
transformers>=4.40
openai>=1.30                    # OpenAI-compatible client (works with vLLM / local endpoints)

# --- interpretability ---
lime>=0.2.0.1

# --- experiment tracking / plotting (optional but recommended) ---
matplotlib>=3.8
tensorboard>=2.15

# --- optional: reuse pyKT baselines & standard splits for comparability ---
# pip install pykt-toolkit
